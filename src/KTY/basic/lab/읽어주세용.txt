1. VO클래스 (정의)만들기!  vo
    멤버변수 선언
    생성자 정의 (기본, 매개변수)
    setter/getter
    toString >>> 대충포멧 "emp{변수1:%?,~~~~~변수n:%?}" 
		 대충결과 string.포멧(포멧,~,~,~,~~~~~)
		result~~~

2. 서비스 클래스에 대한 인터페이스 (정의)만들기
    서비스 클래스에서 구현해야 될 메서드를 정의
    void ~~~~~();
    CRUD 규칙에 따라 메서드에 new, read, readOne, modify, remove라는 접두사 사용

3. 서비스 클래스 정의  ~~~impl
    서비스 인터페이스를 이용해서 CRUD 관련 메서드를 구현
    서비스 클래스는 단일 객체로 생성되어야 하므로 싱글톤 패턴을 포함해서 작성

4. 메인 클래스 정의
    main 메서드를 포함하는 클래스
    프로그램의 실행 진입점